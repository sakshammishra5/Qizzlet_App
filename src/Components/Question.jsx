
const Question = (props) => {
    const {question}=props
    
    // const question=result.map((item,index)=>{item.question})
  return (
    <div>
        {question}
    </div>
  )
}

export default Question